apiVersion: v1
kind: Service
metadata:
  name: {{ include "chdb-ex.fullname" . }}
  labels:
    {{- include "chdb-ex.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chdb-ex.selectorLabels" . | nindent 4 }}
