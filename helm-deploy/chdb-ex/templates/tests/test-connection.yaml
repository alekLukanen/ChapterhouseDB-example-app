apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chdb-ex.fullname" . }}-test-connection"
  labels:
    {{- include "chdb-ex.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chdb-ex.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
